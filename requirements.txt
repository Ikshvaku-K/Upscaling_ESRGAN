# Project dependencies (see pyproject.toml for the installable package).
#
# NOTE: basicsr 1.4.2 imports `torchvision.transforms.functional_tensor`,
# which was removed in torchvision 0.17. Keep torchvision < 0.17 (or patch
# basicsr) until basicsr ships a fix.

# Core inference
torch>=2.0
torchvision>=0.15,<0.17
numpy>=1.24
opencv-python>=4.8
basicsr>=1.4.2
realesrgan>=0.3.0

# Pipeline / utilities
tqdm>=4.66
PyYAML>=6.0.1
psutil>=5.9

# Model download scripts (download_bsrgan.py, download_swinir.py)
# >=2.32.4 fixes CVE-2024-35195 and CVE-2024-47081
requests>=2.32.4

# Comparison / benchmarking scripts (compare_models.py, compare_benchmarks.py)
scikit-image>=0.21
beautifultable>=1.1.0

# Optional: TensorRT acceleration (src/upscaler/core/trt_*.py)
# Install separately, matching your CUDA version:
#   pip install tensorrt
